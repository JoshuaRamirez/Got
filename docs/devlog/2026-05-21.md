# 2026-05-21

## 15:09 UTC — composition Strict step 2: per-side data carrier via projection.Edited

Executed step 2 of the four-step bridge from
`docs/devlog/2026-05-17.md`. Picked the Frontier-with-edits shape over
three-way-with-ancestor on three grounds: smaller API change (no new
Engine method), no need to invent a parent-pointer state model the
codebase doesn't have, and three-way can still be added later on top
of this as a separate `MergeThreeWay` method without conflicting.

### What changed

**`internal/projection/projection.go`**:

- New `Edited` interface — optional extension of `Frontier` that adds
  `VertexEdits() map[VertexID]Vertex` and `EdgeEdits() map[EdgeID]Edge`.
- New concrete `EditedFrontier` struct (with `NewEditedFrontier` ctor)
  that satisfies both `Frontier` and `Edited`.
- Existing `IDsSelector`-derived frontiers continue to work; they do
  not satisfy `Edited` and so are treated as "no per-side data
  available" by the audit.

**`internal/composition/engine.go`**:

- `perSideAudit(left, right)` — runs only when BOTH sides satisfy
  `projection.Edited`. Otherwise returns nil and the strict path falls
  back to in-graph audits only.
- For each shared vertex ID in left and right vertex edits, compares:
  - `Type` → emits `Schema` on disagreement.
  - `Trust` → emits `Trust` on bitwise disagreement.
  - `Time` → emits `Temporal` on bitwise disagreement.
  - `Attrs` per shared key → emits `Textual` on disagreement
    (`attrsEqual` does `==` with `recover` fallback on non-comparable
    types, then `fmt.Sprint` as a conservative last resort).
- For each `(from, to)` pair appearing in both sides' edge edits with
  different types → emits `Structural`.

### What this closes from the trade-off table

The earlier juncture analysis listed seven media of information the
simplified impl was blind to. Strict now covers five at the per-side
level (Schema, Textual, Trust, Temporal, Structural) when both
frontiers carry edits, plus the original two in-graph checks
(Structural via edge collisions in `g`, Temporal via malformed
`TimeTriple` on stored vertices).

Still NOT covered:

- `Capability` — emergence is a higher-layer concern; needs
  `capability.Engine` integration.
- `Evaluation` — same; needs evaluation evidence attached to the
  frontier.
- Three-way semantics — without a common ancestor, the current path
  can't distinguish "both added X" (no conflict) from "ancestor had X,
  left modified, right deleted" (real conflict). Two-way is still
  semantically weaker than three-way; the bridge doesn't close that.

### Equivalence is bitwise

The per-side audits use `==` (plus the recover/fmt fallback for
non-comparable Attrs values). This is conservative — false-positive
prone for Attrs values that are semantically equal but not bitwise
equal (e.g. JSON with different key order). Refining is one of the
open decisions from the juncture; see `docs/devlog/2026-05-17.md`
decision-point #2. Refinement can be additive: replace `attrsEqual`
with a domain-aware predicate when a real need surfaces.

### Tests added

- `TestStrictPerSideSchemaConflict`
- `TestStrictPerSideTextualConflict`
- `TestStrictPerSideTrustConflict`
- `TestStrictPerSideTemporalConflict`
- `TestStrictPerSideStructuralEdgeConflict`
- `TestStrictPerSideAgreement` — identical edits produce no conflicts
- `TestStrictPerSideMixed` — one side `Edited`, one plain: per-side
  audit skipped (audits gated on both sides satisfying `Edited`)

All Lenient tests unchanged. All step-1 Strict tests still pass.
`go build`, `go vet`, `gofmt -l .` clean. `go test -race ./...` green
across all 17 packages.

### Remaining bridge work

| Step | State |
|---|---|
| 1. Strictness flag + in-graph audits | Done (PR #16) |
| 2. Per-side data carrier | Done (this PR) |
| 3. Typed per-medium resolvers | Open |
| 4. Flip default to Strict | Open — gated on (3) and on a deprecation window |

Step 3 (typed resolvers) is the next natural piece. Today
`composition.Resolution` is just `Apply(g) → (g, error)` — generic
graph rewriting. Typed resolvers (`TextualResolver`, `TrustResolver`,
etc.) would let callers say "for any Textual conflict on attr K,
choose left's value" without writing a custom graph mutator. The
shape: introduce `KindedResolution` as a tagged Resolution that the
Resolve loop dispatches on `Conflict.Kind()`.

Step 4 (flip default) is the largest blast radius. The current
default `NewEngine → Lenient` keeps existing callers working unchanged.
Flipping to `NewEngine → Strict` would surface previously-silent
conflicts for any caller using `EditedFrontier` — by then, typed
resolvers (step 3) should be available so callers have a way to
handle the new conflicts. The flip itself is a one-line change plus
a migration window announcement.

## 15:50 UTC — composition Strict step 3: typed payloads and per-medium resolvers

Executed step 3. Three pieces:

**1. Typed payloads on per-side audit conflicts.** Added an optional
`Payloaded` interface (mirrors the `Edited` pattern). `auditConflict`
now satisfies it and carries one of five typed payload structs:
`SchemaPayload`, `TextualPayload`, `TrustPayload`, `TemporalPayload`,
`StructuralPayload`. Each names the offending vertex/edge and both
sides' values. Callers route via type assertion on `Payload()`.

**2. `Resolver` interface and `ResolveTyped` method.** A Resolver
declares `AppliesTo() ConflictKind` and an `Apply` that receives the
graph, the conflict, and the original per-side `*EditedFrontier`s.
`ResolveTyped` dispatches each conflict to the resolver claiming its
kind; unmatched conflicts are left in place to re-surface on the
re-merge.

The Apply signature had to take the per-side frontiers (not just the
graph) because that's where the per-side disagreement lives. A
resolver that says "for this Textual conflict on key K, prefer left"
must mutate right's edit map so the disagreement is gone — mutating
the graph alone doesn't remove the conflict because the audit
compares left.Vertices vs right.Vertices.

**3. Two stock resolvers.**

- `PreferLeftAttr(key)` — for Textual conflicts on the given attr key,
  copies the left value onto right's edit map (and onto the graph if
  the vertex exists there). After this resolver runs, the per-side
  audit for that key sees agreement.
- `PreferHigherTrust()` — for Trust conflicts, sets both sides'
  TrustAnnotation to whichever has the higher Score (ties to Left).

### Design notes

- `Payloaded` is optional. Existing `policyConflict` (the Policy-kind
  conflicts emitted by the governance gate) does not satisfy it. New
  resolver types that want policy-conflict access would need a
  `PolicyPayload` (not added today — no resolver use case).
- `ResolveTyped`'s signature takes `*projection.EditedFrontier` for
  left and right, not the `projection.Frontier` interface, because
  resolvers need to mutate the frontier in place. If the original
  Merge used non-Edited frontiers, callers pass nil and per-side
  resolvers no-op.
- `payloadOf[P]` is a small generic helper for typed access:
  `p, ok := payloadOf[TextualPayload](c)`. Keeps resolver
  implementations short.

### Tests added

- `TestPayloadedTextual` — payload accessible via `Payload()` carries
  the expected typed fields.
- `TestPreferLeftAttrResolvesTextualConflict` — full
  Merge→ResolveTyped→re-Merge cycle ends with zero conflicts.
- `TestPreferHigherTrustResolvesTrustConflict` — same shape for Trust.
- `TestResolveTypedLeavesUnmatchedConflicts` — Trust conflict +
  Textual-only resolver → Trust persists.
- `TestResolveTypedResolverErrors` — failing resolver →
  `ErrConflictUnresolvable`.
- `TestResolveTypedContextCancelled` — ctx cancellation short-circuits.

All existing Lenient + step-1 + step-2 Strict tests still pass.
`go build`, `go vet`, `gofmt -l .` clean. `go test -race ./...` green
across all 17 packages.

### Bridge status

| Step | State |
|---|---|
| 1. Strictness flag + in-graph audits | Done (#16) |
| 2. Per-side data carrier | Done (#17) |
| 3. Typed per-medium resolvers | Done (this PR) |
| 4. Flip default to Strict | Open |

Step 4 is the only one left. The mechanics are trivial (change one
line in `NewEngine`). The judgment is timing: every existing caller
that uses `EditedFrontier` would start seeing typed conflicts where
Lenient currently passes silently. With step 3's resolvers available,
callers have a path to handle the new conflicts — so the flip is
unblocked, but it's a behavior change worth announcing and gating on
a release-notes / changelog mention. Left open until there is a clear
reason to switch the default; the bridge itself is functionally
complete.