# 2026-05-05

## 19:30 UTC — Environment health check

Diagnosed sandbox environment after a series of `Stream idle timeout` and `cache_control cannot be set for empty text blocks` API errors. None were caused by the local environment:

- Disk: 31 GB free of 252 GB (20% used).
- Memory: 14 GB free of 15 GB.
- Go: 1.24.7.
- Working dir: `/home/user/Got`, branch `claude/diagnose-environment-health-AqXKa`, clean tree.

Root cause of the errors was upstream API/MCP transport churn (an MCP server hosting `PushNotification` disconnected mid-session), not a local fault. No remediation needed.

## 19:40 UTC — Branch survey

Inventoried all branches against `origin/main` (`1191c73`):

| Branch | Behind | Ahead | Status |
|---|---|---|---|
| `claude/check-pr-status-hZBcp` | 4 | 0 | Stale, no unique work |
| `claude/formalize-architecture-pn2Nu` | 6 | 1 | Real work, no PR |
| `claude/review-go-quality-qUjl2` | 3 | 2 | Real work, no PR |
| `copilot/create-repo-template-for-go-project` | 7 | 3 | PR #1 closed without merge; obsolete |

PR history: #1 closed-without-merge (template scaffold, superseded), #2 merged (export Query/Match interfaces, fix Branch signature).

## 19:45 UTC — Branch semantics

- `claude/formalize-architecture-pn2Nu` — semantic-grounding spike. Pure additions (~1,531 LOC): `internal/ontology` (schema/types/causal), `internal/identity` (sha256 IDs), `internal/graph` (in-mem impl + tests), `internal/namespace`, `internal/provenance` (engine + tests). No edits to existing code.
- `claude/review-go-quality-qUjl2` — quality cleanup. Small consistency fixes across 14 `internal/` files (~85 ins / 16 del) plus `CLAUDE.md` task list.
- `claude/check-pr-status-hZBcp`, `copilot/create-repo-template-for-go-project` — no live work.

File-level overlap between the two real branches: **none**. Quality branch only edits existing files; architecture branch only adds new ones.

## 19:50 UTC — Merge consolidation onto designated branch

Merged both real branches into `claude/diagnose-environment-health-AqXKa` with `--no-ff`:

- `f75f1da` — Merge `claude/formalize-architecture-pn2Nu`.
- `52219dc` — Merge `claude/review-go-quality-qUjl2`. Auto-merge resolved cleanly on `internal/graph/graph.go`, `internal/repo/repo.go`, `internal/revision/revision.go`.

Verified after combined merge: `go build ./...` clean, `go vet ./...` clean, `go test ./...` all pass (`internal/graph`, `identity`, `namespace`, `ontology`, `provenance`) with `-testcache` cleared. Pushed to `origin/claude/diagnose-environment-health-AqXKa`.

## 19:53 UTC — Fast-forward main

Per explicit user authorization, fast-forwarded `main` to the merged branch tip (no force, no rewrite) and pushed: `1191c73..52219dc main -> main`. Both merge commits now reachable from `main` with full provenance preserved.

## 19:55 UTC — Branch cleanup blocked

Attempted to delete five now-redundant remote branches (`claude/diagnose-environment-health-AqXKa`, `claude/formalize-architecture-pn2Nu`, `claude/review-go-quality-qUjl2`, `claude/check-pr-status-hZBcp`, `copilot/create-repo-template-for-go-project`).

Both attempted channels failed:

- `git push origin --delete <branch>` — sandbox git proxy returned HTTP 403 / disconnect for every branch (including the designated one).
- GitHub MCP server exposed to this session has no `delete_branch` operation (only `create_branch`, `list_branches`, `delete_file`).

Cleanup deferred to the user via GitHub UI or `gh api -X DELETE repos/JoshuaRamirez/Got/git/refs/heads/<branch>`. Do not retry from inside this sandbox until the proxy permissions change.

## 19:55 UTC — Devlog established

Created `docs/devlog/` with `CLAUDE.md` documenting the convention (one file per UTC day, `YYYY-MM-DD.md`, append-only entries with `## HH:MM UTC — Topic` headings) and this file as the first day's log.

## 20:24 UTC — P2 design decisions resolved on `claude/p2-design-decisions`

Cut `claude/p2-design-decisions` from `main` and executed every item from the
root `CLAUDE.md`'s "P2: Open Design Questions" list.

**Rules now codified in `docs/design-rules.md`** (root `CLAUDE.md` slimmed to a
pointer):

1. `context.Context` is the first parameter of every `Engine`/`Service`
   interface method. It is *not* added to pure value-type accessors
   (`Graph`/`Subgraph`/`Frontier`/`View`/...), to `identity.Hasher`/`Factory`
   (deterministic compute), or to `governance.Policy.Check` (Policy is a value,
   not an Engine). `namespace.Store` is the named exception — it gets `ctx` on
   every method because it is the only stateful interface and may be backed by
   a remote store.

2. Sentinel errors are declared at package scope and wrapped via
   `fmt.Errorf("%w: ...", ErrX, detail)`. Added across the codebase:
   `governance.ErrPolicyViolation`, `composition.ErrConflictUnresolvable`,
   `composition.ErrNoPushout`, `verification.ErrCertificationFailed`,
   `verification.ErrEnvironmentMismatch`, `revision.ErrSideConditionFailed`,
   `revision.ErrNoMatch`, `realization.ErrTargetUnsupported`,
   `release.ErrPolicyGate`, `release.ErrUnknownVersion`,
   `replay.ErrNonDeterministic`, `repo.ErrIngestRejected`,
   `temporal.ErrUnknownVertex`, `multiagent.ErrNoAuthorship`,
   `capability.ErrNoEmergence`, `projection.ErrInvalidSelector`,
   `provenance.ErrUnknownVertex`. The pre-existing `graph.Err*` set is kept.

3. Struct-vs-interface rule: single-getter "data holder" types are structs.
   Interfaces are reserved for multi-method contracts, opaque computation
   results, or types with real implementation alternatives. Converted to
   structs: `provenance.Trace`, `replay.Outcome`, `realization.FidelityContract`,
   `capability.Witness`, `composition.MergeWitness`, `multiagent.Responsibility`.
   Multi-method interfaces (`composition.Conflict`, `verification.Certificate`,
   `verification.Evaluation`, etc.) stay as interfaces.

4. `repo.Service.Ingest(State, any)` replaced with
   `Ingest(ctx context.Context, s State, p Payload)`. `Payload` is a typed
   interface with a `PayloadKind() string` discriminator that mirrors
   `graph.Query`.

5. Test gating: every package now has a `*_test.go`. The five concrete-impl
   packages (`graph`, `identity`, `namespace`, `ontology`, `provenance`) keep
   their behavioral tests; the twelve interface-only packages now have minimum
   tests covering exported constants, struct round-trips, and sentinel
   identity. Behavioral tests for those will land with the first concrete
   implementations per the design-rules doc.

**Implementation notes**:
- `provenance.Engine` BFS/DFS now check `ctx.Err()` on every loop iteration.
- `namespace.Store` impls accept `ctx` via blank identifier in `mem.go`
  (idiomatic Go for "accept and propagate" with no local cancellation).
- Two files needed `gofmt -w` after the alignment widths shifted (one error
  variable name, one schema map entry); fixed.
- Added the `Format check` step to `.github/workflows/ci.yml` (`gofmt -l .`
  must be empty). This may fail to push due to the sandbox token's missing
  `workflow` scope; if so, the change will need to be applied manually.

**Verification**: `go build ./...` clean, `go vet ./...` clean,
`go test -race ./...` all 17 packages pass.

## 20:30 UTC — P2 branch fast-forwarded to main

Per explicit user authorization, fast-forwarded `main` to `91f6072` and
pushed: `ec7ed80..91f6072 main -> main`. The P2 refactor is now the tip of
`main`. The `claude/p2-design-decisions` branch and `main` are at the same
commit.

**Surprise of the day**: pushing `.github/workflows/ci.yml` succeeded. The
"needs `workflow` OAuth scope" caveat that the prior root `CLAUDE.md`
warned about does not apply in this sandbox configuration. Removed that
warning when slimming the root CLAUDE.md as part of `91f6072`.

## 21:16 UTC — Documentation map

User asked for a documentation overview. Current state:

- `README.md` — user-facing setup/build/test instructions; untouched today.
- `CLAUDE.md` (root) — thin index, points at `docs/design-rules.md` and
  records the residual gofmt manual-task note.
- `docs/design-rules.md` — canonical API rules (the rules-of-record for new
  and refactored code).
- `docs/devlog/CLAUDE.md` — devlog convention.
- `docs/devlog/YYYY-MM-DD.md` — chronological journal, one file per UTC day.
- In-code: every `internal/*` package has a `package X` doc comment with
  categorical role and allowed-imports declaration; axioms appear inline as
  `// Axiom: ...` comments.

Identified gaps: no top-level architecture overview, no `CHANGELOG.md`, no
test-style guide. Devlog + git log + package doc-comments cover those needs
for now; no action taken.
